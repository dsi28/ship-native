import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  buttonContainer: { marginBottom: 20 },
  stepText: { textAlign: 'center', fontSize: 20, color: 'gray' },
  stepButton: { marginTop: 20 }
});
export default styles;
