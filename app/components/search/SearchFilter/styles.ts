import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  searchContainer: { marginBottom: 15 },
  filterName: { fontSize: 17, fontWeight: 'bold' },
  inputContainer: {
    fontSize: 17,
    borderWidth: 2,
    borderColor: 'lightgray',
    width: '50%'
  }
});
export default styles;
